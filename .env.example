# Canonical URL of this marketing site (no trailing slash).
# On Vercel, set to your marketing deployment URL.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Hirevine product web app (no trailing slash).
# Production default matches `DEFAULT_APP_URL` in `lib/site-config.ts` when omitted.
# Local: point at your dev server, e.g. http://localhost:3000
NEXT_PUBLIC_APP_URL=https://hirevine-web-1026586041764.asia-south1.run.app

# Optional: backend API for product integrations (not used by this marketing site after contact-form removal).
# NEXT_PUBLIC_API_URL=http://localhost:8000
